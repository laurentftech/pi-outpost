# File Specification

> Generated by openlore v1.0.0 on 2026-07-11 03:51

## Purpose

Provides file browsing and search capabilities within a constrained directory structure.

## Requirements

> `server/src/sandbox.ts`

### Requirement: ResolvePath

The system SHALL resolves a file path following symlinks to its deepest existing ancestor.

#### Scenario: ResolvePathWithSymlinks
- **GIVEN** A path containing symlinks
- **WHEN** resolvePath is called
- **THEN** The resolved path without symlinks is returned

### Requirement: CheckPathWithinRoot

> Implementation: `isWithin` in `server/src/sandbox.ts` · confidence: llm

The system SHALL checks if a target path is within a specified root directory.

#### Scenario: PathWithinRoot
- **GIVEN** A target path that is within the root directory
- **WHEN** checkPathWithinRoot is called
- **THEN** Returns true

#### Scenario: PathOutsideRoot
- **GIVEN** A target path that is outside the root directory
- **WHEN** checkPathWithinRoot is called
- **THEN** Returns false

### Requirement: CreateSandboxedTools

The system SHALL creates a set of sandboxed tools for use within a restricted environment.

#### Scenario: CreateToolsWithValidConfig
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** A valid SandboxConfig
- **WHEN** createSandboxedTools is called
- **THEN** Returns an array of ToolDefinition objects


## Sub-components

> `SandboxService` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: RealpathResolution

> Implements: `fs.realpath`

Resolves the absolute path of a file or directory within the sandboxed environment

#### Requirement: ResolvePath

The system SHALL the system shall resolve the absolute path of a given file or directory path within the sandboxed environment

#### Scenario: ResolveRelativePath
- **GIVEN** A relative path './test.txt'
- **WHEN** The system resolves the path
- **THEN** The absolute path '/sandbox/test.txt' is returned

### Sub-component: ReadToolDefinition

> Implements: `createReadToolDefinition`

Creates a tool definition for reading files within the sandboxed environment

#### Requirement: CreateReadTool

The system SHALL the system shall create a tool definition for reading files within the sandboxed environment

#### Scenario: CreateReadTool
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** No inputs
- **WHEN** The system creates the read tool definition
- **THEN** A tool definition for reading files is returned

### Sub-component: LsToolDefinition

> Implements: `createLsToolDefinition`

Creates a tool definition for listing directory contents within the sandboxed environment

#### Requirement: CreateLsTool

The system SHALL the system shall create a tool definition for listing directory contents within the sandboxed environment

#### Scenario: CreateLsTool
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** No inputs
- **WHEN** The system creates the ls tool definition
- **THEN** A tool definition for listing directory contents is returned

### Sub-component: GrepToolDefinition

> Implements: `createGrepToolDefinition`

Creates a tool definition for searching text patterns within files in the sandboxed environment

#### Requirement: CreateGrepTool

The system SHALL the system shall create a tool definition for searching text patterns within files in the sandboxed environment

#### Scenario: CreateGrepTool
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** No inputs
- **WHEN** The system creates the grep tool definition
- **THEN** A tool definition for searching text patterns is returned

### Sub-component: FindToolDefinition

> Implements: `createFindToolDefinition`

Creates a tool definition for finding files and directories within the sandboxed environment

#### Requirement: CreateFindTool

The system SHALL the system shall create a tool definition for finding files and directories within the sandboxed environment

#### Scenario: CreateFindTool
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** No inputs
- **WHEN** The system creates the find tool definition
- **THEN** A tool definition for finding files and directories is returned

### Sub-component: ScopeToRoot

> Implements: `scopeToRoot`

Ensures all paths are scoped to the root directory of the sandboxed environment

#### Requirement: ScopePath

The system SHALL the system shall ensure that all paths are scoped to the root directory of the sandboxed environment

#### Scenario: ScopeRelativePath
- **GIVEN** A relative path '../test.txt'
- **WHEN** The system scopes the path to the root directory
- **THEN** The path is resolved to '/sandbox/test.txt'

### Sub-component: SandboxCreation

> Implements: `create`

Creates a new sandboxed environment with restricted file system operations

#### Requirement: CreateSandbox

The system SHALL the system shall create a new sandboxed environment with restricted file system operations

#### Scenario: CreateNewSandbox
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** No inputs
- **WHEN** The system creates a new sandbox
- **THEN** A new sandboxed environment is created

### Sub-component: PathValidation

> Implements: `isWithin`

Validates that a target path is within the specified root directory

#### Requirement: ValidatePath

The system SHALL the system shall validate that a target path is within the specified root directory

#### Scenario: ValidateWithinRoot
- **GIVEN** A root directory '/sandbox' and a target path '/sandbox/test.txt'
- **WHEN** The system validates the target path
- **THEN** The system returns true indicating the path is within the root directory

### Sub-component: EditToolDefinition

> Implements: `createEditToolDefinition`

Creates a tool definition for editing files within the sandboxed environment

#### Requirement: CreateEditTool

The system SHALL the system shall create a tool definition for editing files within the sandboxed environment

#### Scenario: CreateEditTool
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** No inputs
- **WHEN** The system creates the edit tool definition
- **THEN** A tool definition for editing files is returned

### Sub-component: WriteToolDefinition

> Implements: `createWriteToolDefinition`

Creates a tool definition for writing files within the sandboxed environment

#### Requirement: CreateWriteTool

The system SHALL the system shall create a tool definition for writing files within the sandboxed environment

#### Scenario: CreateWriteTool
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** No inputs
- **WHEN** The system creates the write tool definition
- **THEN** A tool definition for writing files is returned

### Sub-component: ToolRegistration

> Implements: `tools.push`

Registers a new tool definition within the sandboxed environment

#### Requirement: RegisterTool

The system SHALL the system shall register a new tool definition within the sandboxed environment

#### Scenario: RegisterNewTool
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** A tool definition for a new tool
- **WHEN** The system registers the tool definition
- **THEN** The tool definition is added to the list of available tools

### Sub-component: BashToolDefinition

> Implements: `createBashToolDefinition`

Creates a tool definition for executing bash commands within the sandboxed environment

#### Requirement: CreateBashTool

The system SHALL the system shall create a tool definition for executing bash commands within the sandboxed environment

#### Scenario: CreateBashTool
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** No inputs
- **WHEN** The system creates the bash tool definition
- **THEN** A tool definition for executing bash commands is returned

> `server/src/fileBrowser.ts`

### Requirement: ResolveBrowserRoot

The system SHALL resolves the root directory path for the file browser based on the application configuration.

#### Scenario: ValidConfiguration
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Valid AppConfig with a defined root directory
- **WHEN** resolveBrowserRoot is called
- **THEN** The root directory path is resolved successfully

#### Scenario: InvalidConfiguration
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** AppConfig with an undefined root directory
- **WHEN** resolveBrowserRoot is called
- **THEN** An error is thrown indicating the root directory is not defined

### Requirement: ResolveWritableRoot

The system SHALL resolves the writable root directory path relative to the browser root.

#### Scenario: ValidWritableRoot
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Valid AppConfig and browserRoot within the writable zone
- **WHEN** resolveWritableRoot is called
- **THEN** The writable root path is resolved successfully

#### Scenario: InvalidWritableRoot
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Valid AppConfig and browserRoot outside the writable zone
- **WHEN** resolveWritableRoot is called
- **THEN** Null or undefined is returned indicating no writable root

### Requirement: ListDirectory

The system SHALL lists the contents of a directory within the constrained structure.

#### Scenario: ValidDirectory
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Valid root and relPath pointing to an existing directory
- **WHEN** listDirectory is called
- **THEN** The directory contents are listed successfully

#### Scenario: InvalidDirectory
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Valid root and relPath pointing to a non-existent directory
- **WHEN** listDirectory is called
- **THEN** An error is thrown indicating the directory does not exist

### Requirement: ReadFileForPreview

> Implementation: `MAX_PREVIEW_BYTES` in `server/src/fileBrowser.ts` · confidence: semantic

The system SHALL reads a file for preview within the constrained structure.

#### Scenario: ValidFile
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Valid root and relPath pointing to an existing file
- **WHEN** readFileForPreview is called
- **THEN** The file content is read successfully

#### Scenario: InvalidFile
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Valid root and relPath pointing to a non-existent file
- **WHEN** readFileForPreview is called
- **THEN** An error is thrown indicating the file does not exist

### Requirement: SearchFiles

> Implementation: `useTheme` in `web/src/useTheme.ts` · confidence: semantic

The system SHALL searches for files and directories within the constrained structure.

#### Scenario: ValidSearchQuery
<!-- openlore-test: tags=smoke (auto) -->
- **GIVEN** Valid root and query string
- **WHEN** searchFiles is called
- **THEN** The search results are returned successfully

#### Scenario: InvalidSearchQuery
<!-- openlore-test: tags=regression (auto) -->
- **GIVEN** Valid root and empty query string
- **WHEN** searchFiles is called
- **THEN** An empty array is returned indicating no results


## Sub-components

> `FileBrowserService` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: ResolveConfined

> Implements: `resolveConfined`

Resolves a file path within a constrained directory structure.

#### Requirement: ResolvePath

The system SHALL the function shall resolve a given path within the constrained directory structure.

#### Scenario: ValidPath
- **GIVEN** A valid path within the constrained directory
- **WHEN** The function is called with the path
- **THEN** The function SHALL return the resolved path

#### Requirement: ValidatePath

The system SHALL the function shall validate that the resolved path is within the constrained directory structure.

#### Scenario: ValidPath
- **GIVEN** A resolved path within the constrained directory
- **WHEN** The function is called with the resolved path
- **THEN** The function SHALL return true

### Sub-component: FsReaddir

> Implements: `fs.readdir`

Reads the contents of a directory.

#### Requirement: ReadDirectory

The system SHALL the function shall read the contents of a directory and return an array of directory entries.

#### Scenario: ValidDirectory
- **GIVEN** A valid directory path
- **WHEN** The function is called with the directory path
- **THEN** The function SHALL return an array of directory entries

### Sub-component: PromiseAll

> Implements: `Promise.all`

Waits for multiple promises to resolve.

#### Requirement: WaitForPromises

The system SHALL the function shall wait for all provided promises to resolve and return their results.

#### Scenario: ValidPromises
- **GIVEN** An array of valid promises
- **WHEN** The function is called with the array of promises
- **THEN** The function SHALL return an array of resolved results

### Sub-component: DirentIsSymbolicLink

> Implements: `dirent.isSymbolicLink`

Checks if a directory entry is a symbolic link.

#### Requirement: CheckSymbolicLink

The system SHALL the function shall check if the directory entry is a symbolic link and return a boolean.

#### Scenario: SymbolicLink
- **GIVEN** A directory entry that is a symbolic link
- **WHEN** The function is called with the directory entry
- **THEN** The function SHALL return true

### Sub-component: DirentIsDirectory

> Implements: `dirent.isDirectory`

Checks if a directory entry is a directory.

#### Requirement: CheckDirectory

The system SHALL the function shall check if the directory entry is a directory and return a boolean.

#### Scenario: Directory
- **GIVEN** A directory entry that is a directory
- **WHEN** The function is called with the directory entry
- **THEN** The function SHALL return true

### Sub-component: DirentIsFile

> Implements: `dirent.isFile`

Checks if a directory entry is a file.

#### Requirement: CheckFile

The system SHALL the function shall check if the directory entry is a file and return a boolean.

#### Scenario: File
- **GIVEN** A directory entry that is a file
- **WHEN** The function is called with the directory entry
- **THEN** The function SHALL return true

### Sub-component: FsStat

> Implements: `fs.stat`

Retrieves the status of a file or directory.

#### Requirement: GetStat

The system SHALL the function shall retrieve the status of a file or directory and return a stats object.

#### Scenario: ValidPath
- **GIVEN** A valid file or directory path
- **WHEN** The function is called with the path
- **THEN** The function SHALL return a stats object

### Sub-component: Classify

> Implements: `classify`

Classifies a directory entry as a symbolic link, directory, or file.

#### Requirement: ClassifyEntry

The system SHALL the function shall classify the directory entry as a symbolic link, directory, or file and return the classification.

#### Scenario: SymbolicLink
- **GIVEN** A directory entry that is a symbolic link
- **WHEN** The function is called with the directory entry
- **THEN** The function SHALL return 'symbolicLink'

### Sub-component: StatIsDirectory

> Implements: `stat.isDirectory`

Checks if a stats object represents a directory.

#### Requirement: CheckDirectory

The system SHALL the function shall check if the stats object represents a directory and return a boolean.

#### Scenario: Directory
- **GIVEN** A stats object representing a directory
- **WHEN** The function is called with the stats object
- **THEN** The function SHALL return true

### Sub-component: StatIsFile

> Implements: `stat.isFile`

Checks if a stats object represents a file.

#### Requirement: CheckFile

The system SHALL the function shall check if the stats object represents a file and return a boolean.

#### Scenario: File
- **GIVEN** A stats object representing a file
- **WHEN** The function is called with the stats object
- **THEN** The function SHALL return true

### Sub-component: EntriesSort

> Implements: `entries.sort`

Sorts an array of directory entries.

#### Requirement: SortEntries

The system SHALL the function shall sort the array of directory entries and return the sorted array.

#### Scenario: ValidEntries
- **GIVEN** An array of valid directory entries
- **WHEN** The function is called with the array of entries
- **THEN** The function SHALL return the sorted array of entries

### Sub-component: SearchIgnoredNamesHas

> Implements: `SEARCH_IGNORED_NAMES.has`

Checks if a name is in the set of ignored names.

#### Requirement: CheckIgnoredName

The system SHALL the function shall check if the name is in the set of ignored names and return a boolean.

#### Scenario: IgnoredName
- **GIVEN** A name that is in the set of ignored names
- **WHEN** The function is called with the name
- **THEN** The function SHALL return true

### Sub-component: RelPathToLowerCase

> Implements: `relPath.toLowerCase`

Converts a relative path to lowercase.

#### Requirement: ConvertToLowerCase

The system SHALL the function shall convert the relative path to lowercase and return the lowercase path.

#### Scenario: ValidPath
- **GIVEN** A valid relative path
- **WHEN** The function is called with the relative path
- **THEN** The function SHALL return the lowercase path

### Sub-component: ResultsPush

> Implements: `results.push`

Adds an entry to the results array.

#### Requirement: AddEntry

The system SHALL the function shall add the entry to the results array and return the new length of the array.

#### Scenario: ValidEntry
- **GIVEN** A valid entry
- **WHEN** The function is called with the entry
- **THEN** The function SHALL add the entry to the results array and return the new length

### Sub-component: Walk

> Implements: `walk`

Recursively walks through a directory structure and collects file and directory entries.

#### Requirement: WalkDirectory

The system SHALL the function shall recursively walk through the directory structure, collect file and directory entries, and return the results.

#### Scenario: ValidDirectory
- **GIVEN** A valid directory path
- **WHEN** The function is called with the directory path
- **THEN** The function SHALL return an array of collected file and directory entries

## Technical Notes

- **Dependencies**: fs, ToolDefinition, AppConfig
