## 1. Relationship ends in the profile format

- [x] 1.1 Give `relationshipKinds` its own declaration in `shared/schemas/structured-exchange-profile-1.json` with optional `from` and `to` (element kind names, `minItems: 1`), add the types in `shared/src/structuredExchangeProfile.ts`, and copy the schema beside the skill and into the packaged contract; verify a schema test accepts a relationship kind with ends and refuses `from` on an element kind, and the schema copy test passes
- [x] 1.2 Refuse ends naming an undeclared element kind (`profile-format/unresolved-end-kind`, listing declared kinds) and a kind named twice (`profile-format/repeated-end-kind`, pointer to the second) in `structuredExchangeProfileValidation.ts`; verify Node tests for `ARelationshipKindDeclaresItsEnds`, `AnEndNamingAnUndeclaredKindIsRefused`, `AnEndNamingAKindTwiceIsRefused`
- [x] 1.3 Show declared ends in `profileListing` (source and target kinds, or any element kind); verify a test for `DeclaredEndsAreListed` through the validator bundle

## 2. Ends held on documents

- [x] 2.1 Move the document reading of `structuredExchangeRuleEvaluation.ts` (effective items, relationships and their ends, subjects) into a shared module used by the rule evaluator; verify the existing rule-evaluation and conformity-report tests pass unchanged, and the ten-thousand-line batch timing stays within its recorded bound
- [x] 2.2 Check ends in `checkAgainstProfile` (graph edges and table relations, kinds as a proposal leaves them, subjects when stated): `profile/end-kind` refusal at the end's pointer listing allowed kinds, with the "ask, do not pick" guidance; `profile/end-not-verifiable` as a finding; nothing for an undeclared side; verify Node tests for `ARelationshipBetweenKindsItsEndsDoNotAllowIsRefused`, `ATableRelationIsHeldToItsEnds`, `AnUndeclaredSideAllowsAnyKind`, `AnEndOutsideTheDocumentIsAFindingToCheck`, `AProposalIsJudgedByTheKindsItLeaves`
- [x] 2.3 Carry end findings through the agent's tools and the conformance statement; verify `present_structure` tests (refusal text and finding listed) and a UI statement test for `UnverifiableEndsAreCounted`, plus a restored-session server test for `NarrowedEndsApplyToARestoredDocument`
- [x] 2.4 Carry end refusals and findings into batch validation; verify conformity-report tests for `ARelationBetweenDisallowedKindsIsNonConforming` and `AnEndOfUnknownKindIsToCheck`

## 3. Link rules read on declared ends

- [x] 3.1 In `rulesAgainstProfile`, read each conditioned end of a link rule against the relationship kind's declared ends, else every element kind, naming the kinds in the message; verify Node tests for `ALinkRuleConditionIsReadOnTheDeclaredEnds` and `AnUndeclaredEndReadsEveryElementKind`, and that every existing rules-validation test passes

## 4. Generated views

- [x] 4.1 Add the two reserved identifiers to `RESERVED_PROFILE_IDENTIFIERS`; verify tests for `AProfileClaimingAViewIdentifierIsRefused` and `AViewIdentifierIsNeverHeldToAProfile`
- [x] 4.2 Record which files belong to which profile in the registry reader (profile file and the rules files naming it); verify a registry test with two profiles, each with its rules file
- [x] 4.3 Extract the condition formatter from `rulesListing` and implement `rulesRegister` in `shared/src/structuredExchangeProjectViews.ts` (exported as `structured-exchange/project-views`) per the design's shape, with ceiling refusal; verify Node tests for `RulesAreGroupedByTheKindTheyTarget`, `ConditionsReadAsTheyAreChecked`, `TheAttributesARuleSelectsOnAreNamed`, `AForbiddingRuleReadsAsForbidden`, `TheRegisterRecordsItsSources`, `TheRegisterIsReproducible`, `AProfileWithoutRulesSaysSo`, `TheRegisterIsAValidTable`, and that `rulesListing` output is unchanged
- [x] 4.4 Implement `rulePatterns` per the design's shape (a container per rule, typed ends with when / must have, the any-kind end, forbidden relationships, item rules as one element, statements shortened in labels, refusal without rules and past the ceilings), replacing the dropped model map; verify Node tests for `ALinkRuleIsDrawnBetweenItsTypedEnds`, `WhatAnEndMustHaveIsShownApartFromWhatSelectsIt`, `AnUndeclaredEndIsShownAsAnyKind`, `AnItemRuleIsOneElement`, `ALongStatementIsShortenedOnlyInTheLabel`, `AProfileWithoutRulesHasNoPatterns`, `TooManyRulesAreRefusedNotCut`, `ThePatternsAreReproducible`, `ThePatternsAreAValidGraph`
- [x] 4.5 Implement `selectViewProfile` (named, else default, else the only one, else refused listing profiles); verify unit tests for each branch

## 5. The agent tool

- [x] 5.1 Write `server/src/structuredExchangeProjectModelTool.ts` (`present_project_model`) per the design: view envelope in `details`, listing and confirmation request in `content`, error results for no registry, unusable registry, profile choice and ceilings; register it beside `present_structure` in `index.ts` (sandboxed and not) and `piOutpostTools.ts`; verify tool tests for `TheAgentPresentsTheRulesRegister`, `TheAgentPresentsTheRulePatterns`, `TheAgentReadsTheListing`, `AnUnusableRegistryIsReportedIssueByIssue`, `AProjectWithoutARegistryIsToldWhereItGoes`, `SeveralProfilesNeedOneNamed`, `AnEditedRulesFileIsPresentedAsItIsNow`, and the updated tool-list tests
- [x] 5.2 Prove the views render with no new presentation and carry no statement under a default; verify a UI test rendering generated rule patterns (containers and ends drawn) and a server statement test for `AViewIsNeverHeldToTheProjectsProfile`

- [x] 5.3 Publish `present_project_model` on demand: withhold at bind, triggers from prompt text, `tool_start` paths and written content, the setup skill's read, and unusable-registry refusals, aged with the extractors and ordered last with them; the skill and the refusal text point the agent at the registry first; verify unit tests of the triggers and a wire test reading the tool lists sent to a recording provider for `TheToolIsWithheldUntilTheModelIsTouched`, `NamingAModelFilePublishesTheTool`, `WritingAModelFilePublishesTheToolWithinTheTurn`, `ReadingTheSetupSkillPublishesTheTool`, `AnUnusableRegistryRefusalPublishesTheTool`, `AnUncalledToolIsWithdrawn`, then re-measure the context probe

- [x] 5.4 After the live run: publish the tool when the user opens the setup skill, and give the rules file's shape in the refusal of a file that breaks it; tell the person to open the skill in the guide; verify unit and wire tests for `UserOpeningTheSetupSkillPublishesTheTool` and a tool test for `TheRuleFormIsGivenWhenARulesFileBreaksIt`

## 6. The reference validator

- [x] 6.1 Add `--rules-register <file>` and `--rule-patterns <file>` (with `--registry`, profile from `--describe-profile` or the default or the only one, status 4 and nothing written when unusable), document them in the usage text, rebuild the bundle; verify tests against the bundle copied away from the repository for `TheValidatorWritesBothViews`, `AnUnusableRegistryWritesNoView`, and `TheValidatorAndTheToolAgree` comparing the bundle's files with the tool's `details` for the same project

## 7. Skill, examples and documentation

- [x] 7.1 Add examples to the profile schema (a relationship kind with ends) and the rules schema (an item rule and a link rule), and descriptions to `id`, `when` and `then`; verify a test for `TheExamplesAreValid` and `EveryRuleFieldIsDescribed`, and that the copies beside the skill match
- [x] 7.2 Write `skills/structured-exchange-project/SKILL.md` per the spec and design (workflow, the tool as the check, pitfalls, refuse vs report, never inventing vocabulary, complete example files); add the pointer in `skills/structured-exchange/SKILL.md`; update `skills/README.md` and `server/test/bundledSkill.test.ts`; verify bundled-skill tests for `BothBundledSkillsLoad`, `TheSetupSkillIsTurnedOffWithSkills`, `TheSetupSkillIsSelectedForTheProjectsFiles`, `TheAuthoringSkillPointsToTheSetupSkill`, `TheSetupSkillTeachesWhatPassesSilently`, and that every JSON example in the skill passes the documented-examples suite
- [x] 7.3 Write `docs/structured-exchange-project-setup.md` (from a requirements model to reviewed rules, the pitfalls, the two views, the compatibility of ends with older versions) and update `docs/structured-exchange.md` (ends, reserved identifiers, the tool, the validator options), linked both ways; verify every JSON example passes the documented-examples suite and every documented validator command runs through the bundle with its documented status
- [x] 7.4 Check `README.md`, `AGENTS.md`, `embed/README.md` and `docs/` for tool lists, skill lists or validator options this change makes stale, and update them; verify with `rg` for `write_structure_table` and `describe-profile` that every list naming the tools or options includes the new ones

## 8. Verification

- [x] 8.1 Write `scenario-coverage.md` mapping every scenario of the four specs to its test, read each cited assertion, and run `npm run check:scenarios`; verify every scenario is `covered`
- [x] 8.2 Run the full server and UI suites, typecheck, lint and `openspec validate add-structured-exchange-project-setup --strict`; verify all pass
- [x] 8.3 Rebuild `web`, `@pi-outpost/embed` and the e2e host, seed the bench project with a profile declaring ends and a rules file, and drive the running widget: present the rules register and export it, present the rule patterns, present a table with a relation between disallowed kinds (refused) and one to an outside object (finding counted); destructive pass — edit ends to narrow them and reload the session, break then delete the rules file, export the register; verify each by reading back the DOM, and record what broke. Two open projects are covered by a test instead: a bench server holds one project, and the risk there is a cache or a publication belonging to the connection
- [x] 8.4 One short live agent run in the bench: from a prose rule and an existing profile, ask the agent to add the rule and show the register; verify from the session transcript that it called `present_project_model`, and report whether it read the setup skill — done 2026-09-17 with codestral (looped on the SDK's docs tool, never reached the files) and devstral-medium (touched the registry, wrote a rules file, the tool appeared mid-turn and was called unprompted, then ~50 rewrites with invented fields; never opened the skill): led to 5.4; rerun 2026-09-18 with the prompt prefixed by `/skill:structured-exchange-project` (devstral-medium): 9 tool calls, a valid rules file on the first write, one `present_project_model` call presenting the register, then a request to confirm the rule
