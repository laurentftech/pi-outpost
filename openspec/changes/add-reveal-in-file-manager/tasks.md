# Tasks

- [x] 1. `revealPathNative` in `fileBrowser.ts`: confinement, the three invocations, the Linux fallback, Explorer's exit code.
- [x] 2. `reveal_native` in the protocol and `index.ts`.
- [x] 3. `revealNative` in `useAgent.ts`; the control on file and folder rows in `FileTree.tsx`.
- [x] 4. Tests for every scenario; scenario matrix; `npm run check:scenarios`.
- [x] 5. Running app: press the control in the browser and check the launcher call; monkey pass.
- [ ] 6. On Windows and macOS (project owner): the item is shown selected.
