## 1. Protocol and inventory model

- [x] 1.1 Add backward-compatible protocol types for resource provenance, repository groups, discriminated assessment states, revision-bound update requests, and per-workspace reload results; verify shared type checks and protocol serialization tests pass.
- [x] 1.2 Wire the correlated agent-resource messages through the workspace-bound handler: every answer echoes its `requestId`, reaches only the requesting socket, is served for that socket's workspace, and refuses an identifier or token the server did not issue; verify every AgentResourceMessages scenario with two clients and two workspaces on a real server.
- [x] 1.3 Extend runtime snapshots/adapters with skill and extension provenance capability markers, preserving embedded SDK source data and representing missing RPC paths or extension inventory as unavailable; verify adapter tests cover both complete and incomplete runtimes.
- [x] 1.4 Preserve local-folder enrollment inside the manager for both resource kinds, including canonical path deduplication and extension lock/warning behavior; verify Add a local skill folder and Add a local extension folder with settings persistence tests.
- [x] 1.5 Implement confined cloning from a validated repository address into a validated user-visible local folder (with a deterministic managed-directory suggestion), including credential redaction, no shell/options/submodules/hooks, no overwrite, and safe same-origin reuse; verify invalid address/path, failed, occupied, duplicate, and successful clone integration tests.
- [x] 1.6 Implement metadata-only repository preview for recognized skill and extension roots after clone, including fingerprint revalidation, extension-lock filtering, and no module imports; verify every preview/enrollment scenario with a side-effect extension fixture.
- [x] 1.7 Persist confirmed preview roots through existing user skill/extension settings, deduplicate paths, rebuild resources, and merge the repository into inventory; verify mixed, skill-only locked, stale, resource-empty, and re-enrollment integration tests.
- [x] 1.8 Build stable resource identifiers and canonical repository grouping from server-known loaded/configured paths, including symlink resolution, duplicate roots, mixed resources, and nearest nested worktrees; verify focused unit tests cover every Resource provenance inventory scenario.

## 2. Resource repository assessment

- [x] 2.1 Extract or reuse only the Git executable resolution and hardened spawn primitives needed by a new resource-repository service without widening workspace Git commands; verify existing Git confinement tests and the Resource updater does not expand workspace Git authority scenario pass.
- [x] 2.2 Spawn every resource-repository git process non-interactively — no terminal prompt, no askpass helper, no inherited interactive terminal — while leaving configured non-prompting authentication working; verify both Non-interactive git execution scenarios, including one against an address that would prompt, asserting it fails before the command timeout.
- [x] 2.3 Implement the server-owned repository registry with opaque ids and canonical-root validation, rejecting paths or revisions supplied by clients; verify tests prove unknown ids start no Git process and out-of-provenance worktrees never enter the registry.
- [x] 2.4 Implement bounded, per-repository remote refresh and parse clean/current/updateable/dirty/detached/ahead/diverged/no-upstream/locked/unavailable/failed assessments; verify real temporary-repository tests cover all Repository update assessment scenarios, including a repository removed or replaced underneath the inventory, and isolate a failing remote from successful repositories.
- [x] 2.5 Add a small concurrency bound and request correlation to multi-repository refresh; verify a stress test observes the configured bound and keeps each result keyed to its repository.

## 3. Guarded fast-forward updates

- [x] 3.1 Implement revision-bound assessment tokens and a per-canonical-repository mutex, then repeat worktree, branch, upstream, ancestry, extension-lock, and expected-revision checks under the lock; verify stale and concurrent request tests cover Repository changes after assessment, Concurrent updates target one repository, and Confirmation becomes stale.
- [x] 3.2 Implement fetch plus exact-upstream fast-forward integration with fixed argument shapes, no shell, bounded output/time, and a server-owned empty hooks directory; verify real Git integration tests advance only eligible branches and prove repository hooks do not execute.
- [x] 3.3 Enforce the updater-only boundary for dirty and non-fast-forward repositories; verify command-spy and filesystem tests prove no commit, stash, discard, reset, rebase, non-fast-forward merge, push, branch switch, submodule initialization, or client-selected revision occurs, and that a repository carrying a submodule reports its submodule content as not updated.
- [x] 3.4 Enforce executable-code acknowledgement and `extensionLock` server-side for extension-bearing and mixed repositories while leaving inspection and skill-path editing available; verify every Extension update protection and modified ExtensionLock scenario passes, including a direct request bypassing UI controls.

## 4. Workspace lifecycle coordination

- [x] 4.1 Build and maintain the repository-to-workspace reverse index from configured/default root overlap and loaded provenance; verify tests cover repositories shared by several workspaces and resource additions/removals not present in the old inventory.
- [x] 4.2 Reject updates before Git mutation when any affected started workspace is streaming a turn or replacing its session, without starting dormant workspaces; verify Affected workspace is busy and Unstarted workspace loads later with lifecycle integration tests.
- [x] 4.3 After a fast-forward, rebuild every affected started idle runtime, broadcast refreshed inventories, and return per-workspace outcomes; verify Shared repository reloads all affected idle workspaces and multi-client snapshot tests pass.
- [x] 4.4 Preserve and report an advanced repository when any rebuild fails, exposing `updated-reload-failed` without destructive compensation; verify Reload fails after Git succeeds asserts both the new on-disk commit and the explicit partial failure.

## 5. Repository-first interface

- [x] 5.1 Replace Settings' separate path-editing controls with one manager entry point and add the production Agent resources split dialog using prototype A's repository-first layout, with repository/pseudo-group selection and separate skill and extension lists; verify component tests cover Open repository-first resource manager, Settings delegates resource changes to the dialog, and Provenance-unavailable resources stay visible.
- [x] 5.2 Add distinct Add local folder and Add Git repository flows, including directory/type selection, remote-address entry, editable/suggested local clone folder with parent picker, selectable discovery preview, locked extension candidates, apply confirmation, user-root removal, expired/reused-preview errors, stale repository identities after a restart, and focus restoration; verify component tests cover both add actions and repository-enrollment states.
- [x] 5.3 Add search, resource-kind filters, attention filtering, counts, status explanations, external-terminal guidance, and updater-only disabled states; verify component tests cover Search and attention filters preserve repository context and Dirty repository directs resolution outside the app.
- [x] 5.4 Add revision-specific executable-extension confirmation and correlate pending/results by repository and request id; verify component tests cover Extension confirmation precedes update callback and Selection changes during an operation.
- [x] 5.5 Wire local-folder persistence, clone preview, enrollment/removal, refresh, refresh-all, update, reload-result, and snapshot events through application state; verify client/server integration tests exercise one local folder, one skill-only repository, one extension repository, one mixed enrollment, and one non-updateable pseudo-group.

## 6. Documentation and completion proof

- [x] 6.1 Document local-folder enrollment, Git address cloning, where managed clones live on disk and that removing a resource path never deletes them, recognized roots, updater-only Git behavior, dirty-repository remediation, extension confirmation/lock semantics, RPC provenance limitations, clone/fetch authentication, and reload partial failures; verify documented commands and internal links against the implementation.
- [x] 6.2 Enumerate every applicable main and delta `#### Scenario:` with `rg '^#### Scenario:' openspec/` and record the matrix in `openspec/changes/manage-git-backed-agent-resources/scenario-coverage.md` — the file name the CI gate reads — citing the test file and test name for every row, and close every partial or uncovered row before completion.
- [x] 6.3 Run focused unit and integration tests, the relevant server/web suites, type checks, `npm run check:scenarios`, and `openspec validate manage-git-backed-agent-resources --strict`; record the passing commands in the implementation handoff.
- [x] 6.4 Exercise the production feature in the running app with Playwright: complete inventory, refresh, skill update, extension confirmation, lock, dirty, busy, stale-assessment, and reload-failure paths, then monkey-test rapid refresh/update, double clicks, selection/workspace changes mid-request, disappearing repositories, and expand/filter bursts while asserting DOM, filesystem, and session state after each transition.
