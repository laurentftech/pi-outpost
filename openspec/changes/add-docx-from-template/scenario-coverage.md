# Scenario coverage — add-docx-from-template

Capabilities: `docx-templates` (4 requirements, 19 scenarios), `docx-export` (1 requirement,
3 scenarios), `config` (1 requirement, 2 scenarios), `agent` (1 requirement, 3 scenarios).

## docx-templates

| Scenario | Coverage | Evidence |
| --- | --- | --- |
| StylesAreReportedByRole | covered | `server/test/docxBuild.test.ts` — "StylesAreReportedByRole: headings by name under localized ids, with their numbering". Reads `fixtures/docx-template.dotx` (French ids `Titre1`… named `heading 1`…) and asserts, per level, `{ id: "Titre1", name: "heading 1" }` and `numbered` true/true/false for levels 1–3, an absent level 4 reported as absent, the list and body styles, and the formatted line `heading 1: "heading 1" (id Titre1), numbered by the template`. |
| TemplateFeaturesAreReported | covered | `server/test/docxBuild.test.ts` — "TemplateFeaturesAreReported: cover page, header, footer and table of contents". Asserts `features` equals `{ cover: true, tableOfContents: true, headers: true, footers: true, sampleParagraphs: 4 }` and that the answer suggests `keep: ["cover", "toc"]`. |
| AnUnusableWordTemplateIsRefusedWithItsReason | covered | `server/test/docxBuild.test.ts` — "AnUnusableWordTemplateIsRefusedWithItsReason". One refusal per case, each checked against its own message: OLE header (`password-protected or a legacy .doc/.dot`), not a zip (`cannot be read`), a `.potx` (`not a Word document`), the main part retyped macro-enabled (`macro-enabled`), and a DOCTYPE before the root (`damaged: … DOCTYPE`). The DOCTYPE case failed before this change's fix to `readWordPackage`, which only scanned the body. |
| ContentWearsTheTemplatesStyles | covered | `server/test/docxBuild.test.ts` — "ContentWearsTheTemplatesStyles: headings take the template's ids, found by name". Asserts `#`/`##` paragraphs carry `Titre1`/`Titre2`, no `Heading1`/`Heading2` id, the template's `Titre1` definition present verbatim in `styles.xml`, and a level the template lacks (`####`) added as `Heading4` with `outlineLvl 3`. `server/test/wordToolsWire.test.mjs` — "the agent reads the styles, writes a report…" checks the same through the running server. |
| HeadersFootersAndPageSetupAreKept | covered | `server/test/docxBuild.test.ts` — "HeadersFootersAndPageSetupAreKept: the template's final section ends the new body". Asserts the final `sectPr` references `rIdHeader`/`rIdFooter`, keeps the template's margins, `header1.xml` is byte-identical to the template's and its relationship resolves. |
| SampleBodyIsDropped | covered | `server/test/docxBuild.test.ts` — "SampleBodyIsDropped: no sample text, and what only it used is gone". Asserts none of five sample strings survives, and the sample's picture, its relationship and its content type are gone. |
| CoverAndTableOfContentsAreKeptOnRequest | covered | `server/test/docxBuild.test.ts` — "CoverAndTableOfContentsAreKeptOnRequest: both precede the content, and fields refresh on open". Asserts body children 0 and 1 are the `Cover Pages` and `Table of Contents` controls and child 2 the content, `updateFields` placed after `characterSpacingControl` and before `compat`, neither control nor `updateFields` without `keep`, and a warning when the template has no cover. |
| ListsImagesAndTablesSurviveTheGraft | covered | `server/test/docxBuild.test.ts` — "ListsImagesAndTablesSurviveTheGraft: numbering coexists, the picture resolves". Asserts the template's `abstractNum 0`/`num 1` unchanged, every list id in the body > 1 and defined, abstractNums before nums, a `w:tbl`, the image relationship reaching the PNG's exact bytes, the png content type, the external link, and no undeclared namespace prefix in scope anywhere. |
| ATemplateBecomesADocument | covered | `server/test/docxBuild.test.ts` — "ATemplateBecomesADocument: the main part is retyped". Asserts the `document.main+xml` override, no `template.main`, and `isTemplate` false on re-reading. |
| AnExistingDocumentIsKeptUnlessOverwriteIsAsked | covered | `server/test/wordTools.test.ts` — "AnExistingDocumentIsKeptUnlessOverwriteIsAsked: docx_create refuses to write over a file". Calls the tool on an existing `report.docx` without `overwrite`, asserts the refusal and that the file's bytes are unchanged; with `overwrite: true` asserts the file is replaced by a document. |
| PagesHeadingsAndTextAreReported | covered | `server/test/wordTools.test.ts` — "PagesHeadingsAndTextAreReported: page count, the two chapters as bookmarks, a clean text check, the pages". Drives `docx_render` on a machine whose LibreOffice returns the real rendering `fixtures/docx-rendered.pdf`; asserts `1 page(s)`, the bookmarks `1. Findings` / `1.1. Detail` / `2. Next steps` nested as written, `every paragraph of the body is on a page`, and one PNG page picture (or the no-canvas notice). `server/test/wordToolsWire.test.mjs` renders with the real LibreOffice where one is installed. |
| MissingTextIsNamed | covered | `server/test/wordTools.test.ts` — "MissingTextIsNamed: a paragraph on no page is named by the text check". The fixture document carries a `w:vanish` paragraph that LibreOffice did not draw; asserts `Text check — 1 paragraph(s) on no page: "This sentence is hidden text and appears on no page."`. |
| WordIsDrivenWithoutDisturbingTheUser | covered | `server/test/wordTools.test.ts` — "WordIsDrivenWithoutDisturbingTheUser: a read-only private copy, invisible, closed unsaved, Word left running". On a Windows machine: asserts PowerShell is run with `WORD_SCRIPT`, the input is a copy outside the workspace that existed when Word ran, no path in the script; and the script's `Documents.Open(…, $false, $true, $false, …, 0, $missing, $false)` (ReadOnly, not added to recent files, invisible), `ExportAsFixedFormat(…, 17, …, 7, …)`, `Close(0)`, and `Quit(0)` only when `Documents.Count -eq 0`. Word itself is not on CI: the argument positions were checked against the Word VBA reference (MicrosoftDocs/VBA-Docs). |
| ASectionIsReplacedAndTheRestIsUntouched | covered | `server/test/docxUpdate.test.ts` — "ASectionIsReplacedAndTheRestIsUntouched: new body under the old heading, everything else byte-identical". Asserts every body element up to and including the Périmètre heading, and every one from Risques to `sectPr`, is identical to the original; the section's body is the one new paragraph; the part list is the same and `word/document.xml` is the only part whose bytes changed; the removed comment anchor is counted. |
| EditsAreTrackedChangesByDefault | covered | `server/test/docxUpdate.test.ts` — "EditsAreTrackedChangesByDefault: the old text is deleted and the new inserted, by pi-outpost". Without `trackChanges`, asserts the old paragraph survives as `w:delText` in a `w:del`, the new text in a `w:ins`, both paragraph marks as revisions, the section's picture in a `w:del`, no old text outside a deletion, and every revision's author `pi-outpost` and date. `wordToolsWire.test.mjs` checks the same through `docx_update` with its default. |
| AnUnknownOrAmbiguousHeadingIsRefused | covered | `server/test/docxUpdate.test.ts` — "AnUnknownOrAmbiguousHeadingIsRefused: the call fails with the document's headings". Asserts the unknown heading's message and the full indented outline; for two `Détail` sections, the `names 2 sections` message with the outline, the document read left unchanged, and that `Lot B > Détail` then resolves to the second. |
| InsertAndDeleteFollowTheOutline | covered | `server/test/docxUpdate.test.ts` — "InsertAndDeleteFollowTheOutline: a new section after all of Périmètre, and Risques gone with its body". Asserts the resulting outline `Introduction, Périmètre, Inclus, Exclus, Budget, Conclusion`, that Budget follows the table ending Exclus, and that Risques' text and picture reference are gone. |
| PendingRevisionsAreNotEditedOver | covered | `server/test/docxUpdate.test.ts` — "PendingRevisionsAreNotEditedOver: a section holding someone's unaccepted change is refused". On `docx-report-tracked.docx`, asserts the refusal (with its reason) for `Inclus` and for its parent `Périmètre`, and that an edit elsewhere succeeds and keeps the pending insertion. |
| NewContentWearsTheDocumentsStyles | covered | `server/test/docxUpdate.test.ts` — "NewContentWearsTheDocumentsStyles: a subheading takes Titre2 and a list the document's numbering". Asserts `Titre1`/`Titre2` on the inserted headings, both list items on one numId defined in `numbering.xml` with a decimal format, distinct from the document's own list 2, which is unchanged. |

## docx-export

| Scenario | Coverage | Evidence |
| --- | --- | --- |
| ExportUsesTheConfiguredTemplate | covered | `server/test/docxTemplateExport.test.mjs` — "ExportUsesTheConfiguredTemplate: the document comes back in the template's styles, header and footer". A server configured with `docx.template` announces the template by name in `hello`; POSTing the document the browser builds (shared mapping, packed by `docx`) returns a `.docx` whose headings wear `Titre1`/`Titre2`, whose header and footer parts are byte-identical to the template's and referenced, typed as a document, without sample text. `ui/src/components/FileViewer.docxExport.test.tsx` — "ExportUsesTheConfiguredTemplate: offers the template export beside the plain one…" asserts the second button, its title naming the template, and that it sends the viewer's text, path, origin and token; `ui/src/export/docxExport.test.ts` — "buildDocxInTemplate" asserts the request (URL, POST, content type, Bearer token, body a document). |
| WithoutATemplateTheExportIsUnchanged | covered | `ui/src/components/FileViewer.docxExport.test.tsx` — "WithoutATemplateTheExportIsUnchanged: one export, the plain one" asserts a single export button that calls the plain export; `server/test/docxTemplateExport.test.mjs` — "WithoutATemplateTheExportIsUnchanged…" asserts no `docxTemplate` in `hello` and a 404 `no-template` answer. The plain document itself is unchanged: `ui/src/export/docxExport.test.ts` "buildDocx" and `markdownToDocx.test.ts` still pass against the shared mapping. |
| ABrokenTemplateDoesNotBlockThePlainExport | covered | `server/test/docxTemplateExport.test.mjs` — "ABrokenTemplateDoesNotBlockThePlainExport…" asserts 422 with `cannot be read` for a missing template and `the template cannot be read` for a file that is not a zip. `ui/src/components/FileViewer.docxExport.test.tsx` — "ABrokenTemplateDoesNotBlockThePlainExport…" asserts the template button shows the failure with the server's reason, the plain button stays idle and enabled, and a later press of it exports; `docxExport.test.ts` asserts the server's message becomes the error. |

## config

| Scenario | Coverage | Evidence |
| --- | --- | --- |
| PptxRendererKeysStillWork | covered | `server/test/config.test.ts` — "PptxRendererKeysStillWork: the 0.29 pptx.* rendering keys are read, and named as deprecated" asserts `office.renderer`/`libreofficePath`/`renderTimeoutMs` from the `pptx.*` keys and the startup lines naming `pptx.renderer` → `office.renderer`. `server/test/wordToolsWire.test.mjs` — "PptxRendererKeysStillWork: the 0.29 pptx.* keys choose the renderer of documents and decks alike" starts a real server with `pptx.renderer: "libreoffice"` and a LibreOffice path that does not exist, and asserts both `docx_render` and `pptx_render` made exactly one attempt: LibreOffice, at that path. |
| OfficeKeysWinOverTheirAliases | covered | `server/test/config.test.ts` — "OfficeKeysWinOverTheirAliases: office.* wins, and the conflict is logged" asserts `onlyoffice` wins and the line `"pptx.renderer" is ignored: "office.renderer" is set too`. |

## agent

| Scenario | Coverage | Evidence |
| --- | --- | --- |
| NamingAWordTemplatePublishesTheTools | covered | `server/test/documentTools.test.ts` — "NamingAWordTemplatePublishesTheTools…" asserts a `.dotx` prompt yields exactly the four names, spelled out. `server/test/wordToolsWire.test.mjs` — "NamingAWordTemplatePublishesTheTools…" asserts, over a real server, the four are registered but withheld, not sent for a plain prompt, sent on the turn naming `brand.dotx`, and `docx_extract` is not. |
| ReadingTheSkillPublishesTheToolsWithinTheTurn | covered | `server/test/wordToolsWire.test.mjs` — "ReadingTheSkillPublishesTheToolsWithinTheTurn…" asserts the request before the scripted agent reads `skills/docx-from-template/SKILL.md` lacks the four tools and the next request in the same turn carries them, without `docx_extract`. `documentTools.test.ts` covers the matching rule. |
| NoWordCreationInAReadOnlySandbox | covered | `server/test/sandbox-tools.test.ts` — "read-only by default" asserts the exact tool list of a read-only sandbox: `docx_styles`, `docx_render` present, `docx_create`, `docx_update` absent; "adds edit and write only when writing is allowed" asserts they appear with writing. `piOutpostTools.test.ts` asserts the same for the unconfined registration. |
